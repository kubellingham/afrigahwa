/**
 * Afrigahwa's WhatsApp number in international format, digits only
 * (e.g. '255712345678'). While this is empty, the WhatsApp button opens
 * WhatsApp's contact picker with the message pre-filled instead of a
 * direct chat.
 */
export const WHATSAPP_NUMBER = ''
